# Third-party notices

The native executable uses Rust crates distributed under their respective licenses. The release
pipeline must replace the development SBOM with a complete dependency inventory and append all
required license notices before publishing.

Primary browser dependency: `chromiumoxide`, licensed MIT OR Apache-2.0.
