# ChatOS Image Generation

This bundled capability is reserved for a future Local Connector image-model adapter and local post-processing pipeline.

It must remain unavailable until image credentials, model requests, output downloads, file writes, and dependency checks all execute on the Local Connector with an explicit workspace and permission policy. Do not substitute a server-side image generator.
