# 项目质量重构进度表

> 关联计划：`PROJECT_QUALITY_REFACTOR_PLAN.zh-CN.md`
> 更新日期：2026-07-10

| 顺序 | 计划项 | 状态 | 本次处理 | 验证 |
| --- | --- | --- | --- | --- |
| 0 | Phase 0：治理脚本基线 | 已完成 | 新增 `.gitattributes` 固定文本换行为 LF；修复 `scripts/check-non-test-unwrap-expect.py` 显式 UTF-8 读取；规整治理脚本工作区换行；扩展 `scripts/check-large-files.sh` 对多平台 `bundled-tools/*/rg` 的 allowlist。 | `bash scripts/code-size-report.sh --top 50`；`bash scripts/check-hotspot-line-budgets.sh --warn-planned`；`bash scripts/check-large-files.sh --threshold 1 --fail`；`python scripts/check-non-test-unwrap-expect.py`；`git diff --check` |
| 1 | Phase 1.1：HTTP response body 限流下沉 | 已完成 | 在 `crates/chatos_service_runtime/src/http_body.rs` 增加共享流式 body 限流读取 helper；Task Runner 和 Project Management 的本地 `http_body` 模块改为薄 re-export；`chatos_ai_runtime` 复用 preview body helper；保留普通 limit 与 preview limit 两类错误文案。 | `cargo test -p chatos_service_runtime --locked --ignore-rust-version`；`cargo check -p chatos_ai_runtime --locked --ignore-rust-version`；`cargo check -p task_runner_service_backend --locked --ignore-rust-version`；`cargo check -p project_management_service_backend --locked --ignore-rust-version` |
| 2 | Phase 1.2：Frontend Vite 共享 helper | 已完成 | 新增 `scripts/frontend/viteShared.ts`，统一 `parsePort`、`normalizeBasePath`、`basePrefixFromBase` 和 base path proxy rewrite；`user_service`、`task_runner_service`、`project_management_service`、`memory_engine` 四个前端 Vite 配置改为复用共享 helper，同时保留各自 build/test/proxy 目标配置。 | `npm run build` in `user_service/frontend`；`npm run build` in `task_runner_service/frontend`；`npm run build` in `project_management_service/frontend`；`npm run build` in `memory_engine/frontend` |
| 3 | Phase 1.3：Remote connection payload 类型统一 | 已完成 | 将 remote connection create/update/test payload 的单一类型源收敛到 `chatos/frontend/src/lib/api/client/types/remoteConnection.ts`；workspace API client、store action 和 remote execution slice 删除重复 interface，改为 import 或兼容 alias。 | `rg -n "interface RemoteConnectionPayload|interface RemoteConnectionMutationPayload|interface RemoteConnectionCreatePayload|interface CreateRemoteConnectionPayload|interface UpdateRemoteConnectionPayload|Omit<RemoteConnectionPayload" chatos/frontend/src -g "*.ts" -g "*.tsx"`；`npm run build` in `chatos/frontend`；`git diff --check` |
| 4 | Phase 1.4：Code nav 文本搜索共用循环 | 已完成 | 在 `chatos/backend/src/services/code_nav/languages/shared_nav.rs` 新增 `search_text_occurrences`、`TextSearchLine` 和 `TextSearchMatchParts`；Rust、Basic、Java、Python、Go 的 occurrence search 删除重复 regex/WalkDir/budget/file-read/preview 循环，语言层只保留文件过滤、行预处理和 match 构造；Go 保留剥注释后匹配、原始行预览的既有行为。 | `cargo check -p chat_app_server_rs --locked --ignore-rust-version`；`rg -n "RegexBuilder|read_code_nav_file_to_string, truncate_preview|fn should_visit_path|fn should_visit_rust_path|for found in regex.find_iter" chatos/backend/src/services/code_nav/languages -g "*.rs"`；`git diff --check` |
| 5 | Phase 2：Terminal controller response contract | 已完成 | 在 `crates/chatos_builtin_tools/src/terminal_controller_response.rs` 新增 terminal recent logs、process list、process poll、process log、process wait 的共享 DTO/builder，并从 crate 根导出；`local_connector_client/core`、`task_runner_service/backend`、`sandbox_manager_service/sandbox_mcp_server` 三处运行时改为采集 session/meta/log/output 后调用共享 builder，保留 local connector 的 idle/running 判定和各运行时既有日志分页行为；新增共享 builder 单元测试固定关键 JSON 字段。 | `cargo test -p chatos_builtin_tools --locked --ignore-rust-version`；`cargo check -p task_runner_service_backend --locked --ignore-rust-version`；`cargo check -p local_connector_client_core --locked --ignore-rust-version`；`cargo check -p chatos_sandbox_mcp_server --locked --ignore-rust-version`；`rg -n '"result_scope"|"returned_log_count"|"fetched_log_count"|"wait_status"|"output_tail_chars"' local_connector_client/core/src/terminal/controller/store sandbox_manager_service/sandbox_mcp_server/src/terminal_store task_runner_service/backend/src/terminal_store crates/chatos_builtin_tools/src/terminal_controller_response.rs -g "*.rs"`；`git diff --check` |
| 6 | Phase 3：后端大文件拆分 | 已完成 | Phase 3.1 已完成：将 `local_connector_service/backend/src/api.rs` 拆为 `api/mod.rs`、`auth_middleware.rs`、`router.rs`、`devices.rs`、`workspaces.rs`、`project_bindings.rs`、`sandbox_pairings.rs`、`terminal_relay.rs`；认证、路由、设备、workspace、project binding、sandbox pairing、terminal relay 职责已分离。`build_router` 对外导出保持不变，`api/mod.rs` 当前降至 492 行。Phase 3.2 已完成：将 `project_management_service/backend/src/api/harness_mcp.rs` 改为 `api/harness_mcp/mod.rs`；已拆出 `tool_definitions.rs`、`path_policy.rs`、`patch_targets.rs`、`text_edit.rs`、`client.rs`、`tools/read.rs`、`tools/write.rs`、`tools/patch.rs`，`mod.rs` 当前降至 403 行。Phase 3.3 已完成：将 `project_management_service/backend/src/mcp_tools.rs` 改为 `mcp_tools/mod.rs`，已拆出 `pagination.rs`、`conversions.rs`、`project.rs`、`documents.rs`、`requirements.rs` 和 `tasks.rs`，`mod.rs` 当前降至 600 行。Phase 3.4 已完成：从 `services/environment_agent.rs` 拆出 `environment_agent/routing.rs`、`inspection.rs`、`tool_provider.rs`、`memory.rs` 和 `mcp_servers.rs`，集中运行环境路由、Local Connector 项目 root 解析/沙箱配对查询、本地项目 manifest 扫描、Agent 写回工具/env var 生成、Memory Engine 初始化和 MCP server 构造，主文件当前降至 456 行，`mcp_servers.rs` 为 347 行。Phase 3.5 已完成：从 `sandbox_manager_service/backend/src/service/manager.rs` 拆出 `service/output_manifest.rs`，集中 output workspace 导出、change manifest 构造、diff 生成、文件索引/复制 helper 和对应单元测试，主文件当时降至 1669 行，`output_manifest.rs` 当前为 600 行。Phase 3.6 已完成：从 `SandboxManager` 主实现拆出 `service/manager/access_clients.rs`，集中 access client 认证、列表、创建、更新、密钥轮换、删除和 key/hash/scope 规范化 helper，主文件当时降至 1339 行，`access_clients.rs` 为 295 行。Phase 3.7 已完成：从 `SandboxManager` 主实现拆出 `service/manager/mcp_proxy.rs`，集中 MCP proxy 方法、JSON-RPC payload 授权、agent endpoint 校验、agent health probe、JSON-RPC 转发和对应单元测试，主文件当时降至 1041 行，`mcp_proxy.rs` 为 321 行。Phase 3.8 已完成：从 `SandboxManager` 主实现拆出 `service/manager/leases.rs` 和 `lease_inputs.rs`，集中 lease 创建/队列提升/心跳/释放/销毁/列表/事件/过期清理、run workspace 准备和 idempotency 输入规范化，`manager.rs` 当前降至 338 行，`leases.rs` 为 661 行。Phase 3.9 已完成：从 `sandbox_manager_service/backend/src/service/images.rs` 拆出 `service/image_specs.rs`，集中 runtime/version 规格表、feature catalog 构造、feature 规范化和 generated image id 解析，`images.rs` 当前降至 614 行，`image_specs.rs` 为 598 行；Phase 3 相关拆分目录当前最大文件为 661 行。 | `cargo check -p local_connector_service_backend --locked --ignore-rust-version`；`cargo check -p project_management_service_backend --locked --ignore-rust-version`；`cargo check -p sandbox_manager_service_backend --locked --ignore-rust-version`；`cargo test -p sandbox_manager_service_backend --locked --ignore-rust-version`；`git diff --check` |
| 7 | Phase 4：拆分 `local_connector_client/frontend/src/main.tsx` | 已完成 | Phase 4.1 已完成：新增 `local_connector_client/frontend/src/utils/modelConfigState.ts`，抽出 model draft 类型、model config payload builder、provider 分组/标签、thinking options、task model draft/payload 和数值输入解析；`main.tsx` 当时从 2510 行降至 2201 行，`modelConfigState.ts` 为 342 行。Phase 4.2 已完成：新增 `utils/terminalFormat.ts` 和 `utils/approvalFormat.ts`，抽出 terminal history/result 格式化、命令来源/状态标签、审批模式/风险/决策标签和 project label helper；`main.tsx` 当时降至 2089 行。Phase 4.3 已完成：新增 `components/ConnectionPanels.tsx`，抽出登录/注册、连接状态、本机边界、开放目录和目录选择器组件，`main.tsx` 当时降至 1779 行。Phase 4.4 已完成：新增 `components/TerminalPanel.tsx`，抽出终端执行、命令历史刷新/清空、来源过滤和输出预览 UI；`main.tsx` 当时降至 1603 行，`TerminalPanel.tsx` 为 187 行。Phase 4.5 已完成：新增 `components/ApprovalPanel.tsx`，抽出命令审批设置、待审批轮询、白名单和审批历史 UI；`main.tsx` 当时降至 1328 行，`ApprovalPanel.tsx` 为 278 行。Phase 4.6 已完成：新增 `components/RuntimeSettingsPanel.tsx`，抽出 Agent 最大迭代次数运行配置读取和保存 UI；`main.tsx` 当时降至 1237 行，`RuntimeSettingsPanel.tsx` 为 98 行。Phase 4.7 已完成：新增 `components/ModelConfigPanel.tsx`，抽出供应商配置、模型导入、默认模型设置和任务模型设置 UI；`main.tsx` 当时降至 478 行，`ModelConfigPanel.tsx` 为 762 行。Phase 4.8 已完成：新增 `components/SandboxPanel.tsx`，抽出本地沙箱开关、Docker 状态、镜像创建、镜像任务和租约列表 UI；`main.tsx` 当前降至 141 行，`SandboxPanel.tsx` 为 353 行。 | `npm ci` in `local_connector_client/frontend`；`npm run type-check` in `local_connector_client/frontend`；`git diff --check` |
| 8 | Phase 5：整理 `memory_engine_sdk` | 已完成 | 使用 `git mv` 将 `memory_engine/sdk/src/client` 和 `memory_engine/sdk/src/models` 正式迁入 `crates/memory_engine_sdk/src`；`crates/memory_engine_sdk/src/lib.rs` 删除跨目录 `#[path = "../../../memory_engine/sdk/..."]`，改为普通 `mod client; mod models;`；删除旧 `memory_engine/sdk` 独立 Cargo package、Cargo.lock 和旧 lib.rs，只保留 README 指向 shared crate；根 workspace 不再 exclude `memory_engine/sdk`；更新根目录和 `memory_engine/` 下 SDK 使用文档的依赖路径。 | `cargo check -p memory_engine_sdk --locked --ignore-rust-version`；`cargo check -p chatos_ai_runtime --locked --ignore-rust-version`；`cargo check -p task_runner_service_backend --locked --ignore-rust-version`；`cargo check -p project_management_service_backend --locked --ignore-rust-version`；`cargo check -p local_connector_client_core --locked --ignore-rust-version`；`cargo check -p chat_app_server_rs --locked --ignore-rust-version`；`rg -n '#\[path\s*=\s*"\.\./\.\./\.\./memory_engine/sdk|memory_engine/sdk/src|memory_engine/sdk/Cargo.toml|memory_engine/sdk/Cargo.lock' crates memory_engine SDK_USAGE.md Cargo.toml -g '*.rs' -g '*.md' -g 'Cargo.toml'`；`git diff --check` |
| 9 | Phase 6：Workspace 和依赖版本治理 | 已完成 | 新增 `RUST_WORKSPACE_DEPENDENCY_BASELINE.zh-CN.md`，明确当前保留根 workspace + 少量独立服务 workspace 的混合策略，解释 `memory_engine/backend` 和 `user_service/backend` 暂不纳入根 workspace 的原因，并记录 `axum`、`tower-http`、`mongodb` 的当前基线和升级路径；新增 `scripts/check-rust-dependency-drift.py` 扫描所有 `Cargo.toml` 并按 manifest 路径冻结受监控依赖版本；CI 增加 Rust dependency drift 步骤，本地 `scripts/repo-hygiene-report.sh` 建议命令同步加入该检查。 | `python scripts/check-rust-dependency-drift.py`；`bash -n scripts/repo-hygiene-report.sh`；`git diff --check` |
| 10 | Phase 7.1：收敛 planned hotspot `ProjectPlanPane.tsx` | 已完成 | 新增 `projectPlanPane/PlanRequirementColumns.tsx` 和 `projectPlanPane/PlanRequirementDetail.tsx`，将需求列和需求详情 view 从 `ProjectPlanPane.tsx` 拆出；`ProjectPlanPane.tsx` 从 866 行降至 497 行，低于 planned target 500；同步修正 `SessionListModals.test.tsx` 中已过期的 `DirPickerDialog target` 测试 prop，使 `chatos/frontend` type-check 恢复通过。 | `npm run type-check` in `chatos/frontend`；`npm run test -- --run src/components/sessionList/SessionListModals.test.tsx` in `chatos/frontend`；`bash scripts/check-hotspot-line-budgets.sh --warn-planned` |
| 11 | Phase 7.2：收敛 planned hotspot `ProjectRunSettingsPanel.tsx` | 已完成 | 新增 `projectRunSettingsPanel/types.ts`，将 `ProjectRunSettingsPanelProps` 从组件文件移出为独立 contract；`ProjectRunSettingsPanel.tsx` 从 512 行降至 449 行，低于 planned target 500。 | `npm run type-check` in `chatos/frontend`；`bash scripts/check-hotspot-line-budgets.sh --warn-planned` |
| 12 | Phase 7.3：收敛 planned hotspot i18n messages | 已完成 | 新增 `enUSWorkspace.ts` 和 `zhCNWorkspace.ts`，将 `remote`、`projectExplorer`、`runSettings`、`git`、`inputArea` namespace 文案从主语言包拆出；`enUS.ts` 和 `zhCN.ts` 均从 1723 行降至 1090 行，低于 planned target 1200，文案 key 集合保持一致。 | `npm run type-check` in `chatos/frontend`；en/zh key parity script；`bash scripts/check-hotspot-line-budgets.sh --warn-planned`；`git diff --check` |
| 13 | Phase 8.1：收敛额外热点 `user_service/backend/src/integrations.rs` | 已完成 | 将 1592 行 `integrations.rs` 拆成 `integrations/mod.rs`、`harness.rs`、`harness/identifiers.rs`、`harness/repo.rs`、`model_sync.rs` 和 `http.rs`；Harness provisioning、Harness repo/API access、标识符规范化、模型配置同步和共享 HTTP helper 职责分离；同步补齐 `user_service/backend/Cargo.lock`，使该独立 workspace 可 `--locked` 编译。 | `cargo check --locked --ignore-rust-version` in `user_service/backend`；`cargo test --locked --ignore-rust-version harness` in `user_service/backend`；`bash scripts/code-size-report.sh --top 30`；`bash scripts/check-hotspot-line-budgets.sh --warn-planned`；`git diff --check` |
| 14 | Phase 8.2：收敛额外热点 `task_runner_service/backend/src/services/sandbox_runtime.rs` | 已完成 | 将 1285 行 `sandbox_runtime.rs` 拆出 `sandbox_runtime/output.rs`、`workspace.rs` 和 `manager_client.rs`；主文件保留 RunService 沙箱编排和上下文 metadata/MCP server 构造，输出清单/diff 读取、workspace baseline 复制、Sandbox Manager HTTP lease/health/release client 分别独立；主文件降至 641 行，拆出文件最大 340 行。 | `cargo check -p task_runner_service_backend --locked --ignore-rust-version`；`cargo test -p task_runner_service_backend --locked --ignore-rust-version sandbox_runtime`；`bash scripts/code-size-report.sh --top 30`；`bash scripts/check-hotspot-line-budgets.sh --warn-planned`；`git diff --check` |
| 15 | Phase 8.3：收敛额外热点 `task_runner_service/backend/src/services/workspace_mcp.rs` | 已完成 | 将 1167 行 `workspace_mcp.rs` 拆出 `workspace_mcp/workspace_dirs.rs` 和 `workspace_mcp/tests.rs`；主文件保留 MCP 运行时路由、内置能力选择和 local/harness ephemeral server 构造，workspace 目录解析/安全边界检查和大块单测独立；主文件降至 522 行，拆出文件最大 427 行。 | `cargo check -p task_runner_service_backend --locked --ignore-rust-version`；`cargo test -p task_runner_service_backend --locked --ignore-rust-version workspace_mcp`；`bash scripts/code-size-report.sh --top 30`；`bash scripts/check-hotspot-line-budgets.sh --warn-planned`；`git diff --check` |
| 16 | Phase 8.4：收敛额外热点 `chatos/backend/src/api/local_connectors.rs` | 已完成 | 将 1120 行 `local_connectors.rs` 拆出 `local_connectors/types.rs`、`root_path.rs`、`connector_client.rs` 和 `directory_payload.rs`；主文件保留 API route handler、项目创建/绑定和 terminal/MCP orchestration，Local Connector DTO、root path 编解码、HTTP relay client 和目录列表 payload 构造独立；主文件降至 684 行，拆出文件最大 148 行。 | `cargo check -p chat_app_server_rs --locked --ignore-rust-version`；`bash scripts/code-size-report.sh --top 30`；`bash scripts/check-hotspot-line-budgets.sh --warn-planned`；`git diff --check` |
| 17 | Phase 8.5：收敛额外热点 `crates/chatos_mcp_runtime/src/rpc.rs` | 已完成 | 将 929 行 `rpc.rs` 拆出 `rpc/stdio.rs`，集中 stdio JSON-RPC 调用、session cache/start lock、进程启动和响应行限流；父文件保留 HTTP JSON-RPC、tools/list cache 和公共 re-export；`rpc.rs` 降至 569 行，`stdio.rs` 为 377 行。 | `cargo check -p chatos_mcp_runtime --locked --ignore-rust-version`；`cargo test -p chatos_mcp_runtime --locked --ignore-rust-version rpc`；`bash scripts/code-size-report.sh --top 30`；`bash scripts/check-hotspot-line-budgets.sh --warn-planned`；`git diff --check` |
| 18 | Phase 8.6：收敛额外热点 `crates/chatos_service_runtime/src/lib.rs` | 已完成 | 将 866 行 `lib.rs` 拆出 `config.rs`、`consul.rs`、`env_config.rs`、`error.rs`、`http_client.rs`、`local_model_runtime.rs`、`runtime.rs` 和 `utils.rs`；根文件只保留公共 re-export 与默认常量，Consul 注册/发现、配置中心环境注入、本地模型运行时解析、HTTP client 构造和测试随职责迁移；`lib.rs` 降至 34 行，拆出文件最大为 `runtime.rs` 376 行。 | `cargo test -p chatos_service_runtime --locked --ignore-rust-version`；`cargo check -p chatos_ai_runtime --locked --ignore-rust-version`；`cargo check -p task_runner_service_backend --locked --ignore-rust-version`；`cargo check -p project_management_service_backend --locked --ignore-rust-version`；`cargo check -p chat_app_server_rs --locked --ignore-rust-version`；`cargo check -p local_connector_service_backend --locked --ignore-rust-version`；`cargo check -p sandbox_manager_service_backend --locked --ignore-rust-version`；`bash scripts/code-size-report.sh --top 30`；`bash scripts/check-hotspot-line-budgets.sh --warn-planned`；`git diff --check` |
| 19 | Phase 8.7：收敛额外热点 `crates/chatos_builtin_tools/src/remote_connection_controller.rs` | 已完成 | 将 827 行 `remote_connection_controller.rs` 拆出 `remote_connection_controller/types.rs`、`tools.rs`、`args.rs` 和 `tests.rs`；根文件保留服务 façade、公共 re-export 和注册编排，公共 contract/store ref、7 个远程连接工具 schema/handler、参数解析和单测分别独立；主文件降至 114 行，拆出文件最大为 `tools.rs` 335 行。 | `cargo test -p chatos_builtin_tools --locked --ignore-rust-version remote_connection_controller`；`cargo check -p chatos_builtin_tools --locked --ignore-rust-version`；`cargo check -p chat_app_server_rs --locked --ignore-rust-version`；`cargo check -p task_runner_service_backend --locked --ignore-rust-version`；`bash scripts/code-size-report.sh --top 30`；`bash scripts/check-hotspot-line-budgets.sh --warn-planned`；`git diff --check` |
| 20 | Phase 8.8：收敛额外热点 `local_connector_client/core/src/model_configs/service.rs` | 已完成 | 新增 `model_configs/provider_catalog.rs`，将 provider 规范化、默认 base URL、运行时 provider 判定、模型列表拉取/解析、fallback model list 和 catalog 单测从 `service.rs` 拆出；`service.rs` 保留本地配置 CRUD、user_service 同步、运行时解析和 relay handler，主文件从 852 行降至 609 行，`provider_catalog.rs` 为 259 行。 | `cargo test -p local_connector_client_core --locked --ignore-rust-version model_configs`；`cargo check -p local_connector_client_core --locked --ignore-rust-version`；`bash scripts/code-size-report.sh --top 30`；`bash scripts/check-hotspot-line-budgets.sh --warn-planned`；`git diff --check` |
| 21 | Phase 8.9：收敛额外热点 `chatos/backend/src/api/projects/run_handlers.rs` | 已完成 | 新增 `run_handlers/local_connector.rs` 和 `run_handlers/visibility.rs`，将 Local Connector 项目运行派发、终端创建/复用/输入发送、运行实例实时事件，以及 project run catalog/environment/terminal/env overrides 的可见路径序列化从 handler 文件拆出；`run_handlers.rs` 保留 API handler、权限检查、catalog 加载和执行编排，主文件从 806 行降至 539 行，拆出文件最大为 202 行。 | `cargo check -p chat_app_server_rs --locked --ignore-rust-version`；`bash scripts/code-size-report.sh --top 30`；`bash scripts/check-hotspot-line-budgets.sh --warn-planned`；`git diff --check` |
| 22 | Phase 8.10：收敛额外热点 `project_management_service/backend/src/services/execution_sync.rs` | 已完成 | 新增 `execution_sync/status_transition.rs`，将 Task Runner 状态到 work item 状态映射、完成/失败/阻塞 work item 对关联 requirement 的状态传播、requirement 子树收集和状态可迁移规则从同步入口文件拆出；`execution_sync.rs` 保留同步 API 入口、link upsert、requirement execution state 同步和单测，主文件从 790 行降至 577 行，`status_transition.rs` 为 236 行。 | `cargo test -p project_management_service_backend --locked --ignore-rust-version execution_sync`；`cargo check -p project_management_service_backend --locked --ignore-rust-version`；`bash scripts/code-size-report.sh --top 30`；`bash scripts/check-hotspot-line-budgets.sh --warn-planned`；`git diff --check` |
| 23 | Phase 8.11：收敛额外热点 `chatos/backend/src/api/fs/policy.rs` | 已完成 | 新增 `api/fs/policy/tests.rs`，将路径策略单测从实现文件拆出；`policy.rs` 从 765 行降至 419 行，`policy/tests.rs` 为 347 行；同步修正若干过期的后端测试构造，使 `chat_app_server_rs` lib-test 编译恢复通过。 | `cargo test -p chat_app_server_rs --locked --ignore-rust-version fs::policy`；`cargo check -p chat_app_server_rs --locked --ignore-rust-version`；`bash scripts/code-size-report.sh --top 30`；`bash scripts/check-hotspot-line-budgets.sh --warn-planned`；`git diff --check` |
| 24 | Phase 8.12：收敛额外热点 `project_management_service/backend/src/task_runner_api_client.rs` | 已完成 | 新增 `task_runner_api_client/tests.rs`，将 Task Runner internal execution options 和 create-task client 单测从实现文件拆出；`task_runner_api_client.rs` 从 719 行降至 380 行，`tests.rs` 为 341 行，运行时请求构造逻辑保持在主文件。 | `cargo test -p project_management_service_backend --locked --ignore-rust-version task_runner_api_client`；`cargo check -p project_management_service_backend --locked --ignore-rust-version`；`bash scripts/code-size-report.sh --top 30`；`bash scripts/check-hotspot-line-budgets.sh --warn-planned`；`git diff --check` |
| 25 | Phase 8.13：收敛额外热点 `crates/chatos_builtin_tools/src/code_maintainer/fs_ops.rs` | 已完成 | 新增 `code_maintainer/fs_ops/tests.rs`，将文件删除、搜索、范围读取等 FsOps 单测从实现文件拆出；`fs_ops.rs` 从 710 行降至 582 行，`tests.rs` 为 130 行，读写/搜索实现逻辑保持不变。 | `cargo test -p chatos_builtin_tools --locked --ignore-rust-version fs_ops`；`cargo check -p chatos_builtin_tools --locked --ignore-rust-version`；`bash scripts/code-size-report.sh --top 30`；`bash scripts/check-hotspot-line-budgets.sh --warn-planned`；`git diff --check` |
| 26 | Phase 8.14：收敛额外热点 `task_runner_service/backend/src/services/mcp_resolution.rs` | 已完成 | 新增 `services/mcp_resolution/tests.rs`，将内置 MCP 能力依赖补全、固定工具来源、host route 和 legacy header 恢复测试从实现文件拆出；`mcp_resolution.rs` 从 729 行降至 440 行，`tests.rs` 为 291 行，运行时解析与路由逻辑保持在主文件。 | `cargo test -p task_runner_service_backend --locked --ignore-rust-version mcp_resolution`；`cargo check -p task_runner_service_backend --locked --ignore-rust-version`；`bash scripts/code-size-report.sh --top 30`；`bash scripts/check-hotspot-line-budgets.sh --warn-planned`；`git diff --check` |
| 27 | Phase 8.15：收敛额外热点 `chatos/backend/src/services/project_run/analyzer.rs` | 已完成 | 新增 `project_run/analyzer/tests.rs`，将 Local Connector Maven/Spring Boot target 识别单测从分析实现文件拆出；因 `project_run/mod.rs` 使用 `#[path = "analyzer.rs"]`，测试模块显式指向 `analyzer/tests.rs`；`analyzer.rs` 从 732 行降至 679 行，`tests.rs` 为 56 行。 | `cargo test -p chat_app_server_rs --locked --ignore-rust-version project_run::analyzer`；`cargo check -p chat_app_server_rs --locked --ignore-rust-version`；`bash scripts/code-size-report.sh --top 30`；`bash scripts/check-hotspot-line-budgets.sh --warn-planned`；`git diff --check` |
| 28 | Phase 8.16：收敛额外热点 `chatos/backend/src/services/code_nav/languages/shared_nav.rs` | 已完成 | 新增 `shared_nav/symbols.rs`，将 `NavSymbolLike`/`NavSearchMatchLike`、字段结构实现宏、indexed symbol 转换和 document-symbol response 构造从共享导航主文件拆出；`shared_nav.rs` 从 732 行降至 619 行，`symbols.rs` 为 125 行，父模块继续 re-export 原有符号。 | `cargo test -p chat_app_server_rs --locked --ignore-rust-version code_nav::languages`；`cargo check -p chat_app_server_rs --locked --ignore-rust-version`；`bash scripts/code-size-report.sh --top 30`；`bash scripts/check-hotspot-line-budgets.sh --warn-planned`；`git diff --check` |
| 29 | Phase 8.17：收敛额外热点 `local_connector_service/backend/src/store.rs` | 已完成 | 新增 `store/mongo.rs`，将 `MongoConnectorStore`、MongoDB index 创建和设备/工作区/项目绑定/沙箱配对/session CRUD 查询实现从 store façade 文件拆出；`store.rs` 从 754 行降至 231 行，`mongo.rs` 为 534 行。 | `cargo test -p local_connector_service_backend --locked --ignore-rust-version store`；`cargo check -p local_connector_service_backend --locked --ignore-rust-version`；`bash scripts/code-size-report.sh --top 30`；`bash scripts/check-hotspot-line-budgets.sh --warn-planned`；`git diff --check` |
| 30 | Phase 8.18：收敛额外热点 `local_connector_client/core/src/approval/ai_agent.rs` | 已完成 | 新增 `approval/ai_agent/tool_executor.rs`，将命令审批 Agent 的 ToolExecutor 实现、审批决策工具 schema、只读 code maintainer 工具白名单执行、工具参数解析和结果回调封装从 agent 编排文件拆出；`ai_agent.rs` 从 785 行降至 504 行，`tool_executor.rs` 为 295 行。 | `cargo test -p local_connector_client_core --locked --ignore-rust-version approval::ai_agent`；`cargo check -p local_connector_client_core --locked --ignore-rust-version`；`bash scripts/code-size-report.sh --top 30`；`bash scripts/check-hotspot-line-budgets.sh --warn-planned`；`git diff --check` |
| 31 | Phase 8.19：收敛额外热点 `local_connector_client/frontend/src/components/ModelConfigPanel.tsx` | 已完成 | 新增 `TaskModelSettingsSection.tsx`，将任务模型设置面板的 drafts 状态、保存逻辑和 UI 从模型配置主面板拆出；`ModelConfigPanel.tsx` 从 764 行降至 611 行，`TaskModelSettingsSection.tsx` 为 164 行。 | `npm run type-check` in `local_connector_client/frontend`；`bash scripts/code-size-report.sh --top 30`；`bash scripts/check-hotspot-line-budgets.sh --warn-planned`；`git diff --check` |
| 32 | Phase 8.20：收敛额外热点 `chatos/frontend/src/components/projectExplorer/projectPlanPane/components.tsx` | 已完成 | 新增 `RequirementExecutionPreviewModal.tsx`，将需求执行/流程预览 modal、preview graph 布局和边绘制 helper 从通用 components 文件拆出，并在原文件 re-export 保持外部导入路径不变；`components.tsx` 从 844 行降至 409 行，`RequirementExecutionPreviewModal.tsx` 为 454 行。 | `npm run type-check` in `chatos/frontend`；`bash scripts/code-size-report.sh --top 30`；`bash scripts/check-hotspot-line-budgets.sh --warn-planned`；`git diff --check` |
| 33 | Phase 8.21：收敛额外热点 `chatos/frontend/src/components/sessionList/useSessionListController.ts` | 已完成 | 新增 `useLocalConnectorResources.ts`，将 Local Connector workspace/目录列表归一化、目录浏览、目录创建和相关本地状态从 Session List 主 controller 中拆出；`useSessionListController.ts` 从 858 行降至 639 行，`useLocalConnectorResources.ts` 为 272 行。 | `npm run type-check` in `chatos/frontend`；`bash scripts/code-size-report.sh --top 30`；`bash scripts/check-hotspot-line-budgets.sh --warn-planned`；`git diff --check` |
| 34 | Phase 8.22：收敛额外热点 `task_runner_service/frontend/src/i18n/messages/enUS.ts` / `zhCN.ts` | 已完成 | 新增 `enUSSettings.ts` 和 `zhCNSettings.ts`，将 settings/MCP catalog/runs/tooling/projects 管理视图文案从 Task Runner 主语言包拆出；`enUS.ts` 从 735 行降至 296 行，`zhCN.ts` 从 735 行降至 295 行，拆出文件均为 449 行。 | `npm run type-check` in `task_runner_service/frontend`；`bash scripts/code-size-report.sh --top 30`；`bash scripts/check-hotspot-line-budgets.sh --warn-planned`；`git diff --check` |
| 35 | Phase 8.23：收敛额外热点 `task_runner_service/backend/src/mcp_server/tests/plan_profile.rs` | 已完成 | 新增 `plan_profile_async.rs`，将 Chatos async planner 复用、MCP agent 执行状态保护和已完成任务不复用测试从 plan profile 主测试文件拆出；`plan_profile.rs` 从 1086 行降至 683 行，热点报告总数从 7 降至 6，剩余热点集中在 CSS 和 Chatos i18n 大文件。 | `cargo test -p task_runner_service_backend --locked --ignore-rust-version plan_profile`；`cargo check -p task_runner_service_backend --locked --ignore-rust-version`；`bash scripts/code-size-report.sh --top 30`；`bash scripts/check-hotspot-line-budgets.sh --warn-planned`；`git diff --check` |
| 36 | Phase 8.24：收敛额外热点 `chatos/frontend/src/i18n/messages/*.ts` | 已完成 | 新增 `enUSManagement.ts` / `zhCNManagement.ts`，将 settings、applications、notepad、agent/system/model/MCP manager、tool summary 等管理类文案从主语言包拆出；新增 `enUSRemote.ts` / `zhCNRemote.ts`，将 remote/SFTP 文案从 workspace 语言包拆出；`enUS.ts` / `zhCN.ts` 降至 629 行且小于 40KB，`enUSWorkspace.ts` / `zhCNWorkspace.ts` 降至 558 行且小于 40KB，热点报告总数从 6 降至 2。 | `npm run type-check` in `chatos/frontend`；en/zh key parity scan（1716 keys each, 0 diff）；`bash scripts/code-size-report.sh --top 30`；`bash scripts/check-hotspot-line-budgets.sh --warn-planned`；`git diff --check` |
| 37 | Phase 8.25：收敛额外热点 `local_connector_client/frontend/src/styles.css` | 已完成 | 将 Local Connector 前端全局样式按原 cascade 顺序拆成 `styles-terminal.css`、`styles-approval.css`、`styles-models.css`、`styles-command-history.css`、`styles-sandbox.css` 和 `styles-responsive.css`；入口 `main.tsx` 按原顺序导入，选择器和声明不改；`styles.css` 从 1230 行降至 310 行，拆出 CSS 最大 268 行，热点报告总数从 2 降至 1。 | `npm run type-check` in `local_connector_client/frontend`；combined CSS body vs HEAD scan（忽略空行一致）；`bash scripts/code-size-report.sh --top 30`；`bash scripts/check-hotspot-line-budgets.sh --warn-planned`；`git diff --check` |
| 38 | Phase 8.26：收敛额外热点 `official_website_service/frontend/src/styles.css` | 已完成 | 将官网前端全局样式按原 cascade 顺序拆成 `styles-status.css`、`styles-stack.css` 和 `styles-responsive.css`；入口 `main.tsx` 按原顺序导入，选择器和声明不改；`styles.css` 从 893 行降至 377 行，拆出 CSS 最大 217 行，热点报告已降至 0。 | `npm run type-check` in `official_website_service/frontend`；combined CSS body vs HEAD scan（忽略空行一致）；`bash scripts/code-size-report.sh --top 30`；`bash scripts/check-hotspot-line-budgets.sh --warn-planned`；`git diff --check` |
