// SPDX-License-Identifier: PolyForm-Noncommercial-1.0.0
// Required Notice: Copyright (c) 2025 AI Chat Team

import type { useProjectExplorerPreviewNavigation } from './useProjectExplorerPreviewNavigation';
import type { useProjectExplorerWorkspaceActions } from './useProjectExplorerWorkspaceActions';
import type { useProjectExplorerWorkspaceViewModel } from './useProjectExplorerWorkspaceViewModel';
import type { UseProjectExplorerWorkspaceModelParams } from './workspaceModelTypes';

type WorkspaceModelActions = ReturnType<typeof useProjectExplorerWorkspaceActions>;
type WorkspaceModelNavigation = ReturnType<typeof useProjectExplorerPreviewNavigation>;

export const buildWorkspaceActionsParams = ({
  project,
  client,
  state,
  pathHelpers,
  dataLoading,
  selection,
  treeStateOps,
}: UseProjectExplorerWorkspaceModelParams): Parameters<typeof useProjectExplorerWorkspaceActions>[0] => ({
  dnd: {
    treeScrollRef: state.treeScrollRef,
    entriesMap: state.entriesMap,
    loadingPaths: state.loadingPaths,
    normalizePath: pathHelpers.normalizePath,
    toExpandedKey: pathHelpers.toExpandedKey,
    getParentPath: pathHelpers.getParentPath,
    findEntryByPath: selection.findEntryByPath,
    loadEntries: dataLoading.loadEntries,
    setExpandedPaths: state.setExpandedPaths,
  },
  treeActions: {
    client,
    selectedDirPath: selection.selectedDirPath,
    selectedEntry: selection.selectedEntry,
    selectedFilePath: state.selectedFile?.path || null,
    projectRootPath: project?.rootPath,
    actionReloadPath: selection.actionReloadPath,
    normalizePath: pathHelpers.normalizePath,
    getParentPath: pathHelpers.getParentPath,
    toExpandedKey: pathHelpers.toExpandedKey,
    loadEntries: dataLoading.loadEntries,
    pruneDeletedPath: treeStateOps.pruneDeletedPath,
    replaceExpandedPathPrefix: treeStateOps.replaceExpandedPathPrefix,
    reloadTreeWithExpanded: treeStateOps.reloadTreeWithExpanded,
    findEntryByPath: selection.findEntryByPath,
    setExpandedPaths: state.setExpandedPaths,
    setSelectedPath: state.setSelectedPath,
    setSelectedFile: state.setSelectedFile,
    setActionLoading: state.setActionLoading,
    setActionError: state.setActionError,
    setActionMessage: state.setActionMessage,
    setSavingFile: state.setSavingFile,
    setSaveError: state.setSaveError,
    setMoveConflict: state.setMoveConflict,
    openFile: selection.openFile,
  },
});

export const buildWorkspaceEffectsParams = (
  {
    project,
    filesTabActive,
    state,
    pathHelpers,
    dataLoading,
  }: UseProjectExplorerWorkspaceModelParams,
  actions: Pick<WorkspaceModelActions, 'clearDragExpandTimer' | 'clearDragAutoScroll'>,
) => ({
  lifecycle: {
    projectId: project?.id,
    projectRootPath: project?.rootPath,
    filesTabActive,
    toExpandedKey: pathHelpers.toExpandedKey,
    keyToPath: pathHelpers.keyToPath,
    loadEntries: dataLoading.loadEntries,
    tryLoadEntries: dataLoading.tryLoadEntries,
    clearDragExpandTimer: actions.clearDragExpandTimer,
    clearDragAutoScroll: actions.clearDragAutoScroll,
    setEntriesMap: state.setEntriesMap,
    setLoadingPaths: state.setLoadingPaths,
    setExpandedPaths: state.setExpandedPaths,
    setSelectedPath: state.setSelectedPath,
    setSelectedFile: state.setSelectedFile,
    setActionMessage: state.setActionMessage,
    setActionError: state.setActionError,
    setActionLoading: state.setActionLoading,
    setContextMenu: state.setContextMenu,
    setMoveConflict: state.setMoveConflict,
    setDraggingEntryPath: state.setDraggingEntryPath,
    setDropTargetDirPath: state.setDropTargetDirPath,
    setExpandedReady: state.setExpandedReady,
  },
  persistence: {
    projectId: project?.id,
    projectRootPath: project?.rootPath,
    expandedReady: state.expandedReady,
    expandedPaths: state.expandedPaths,
    workspaceTab: state.workspaceTab,
    setWorkspaceTab: state.setWorkspaceTab,
    contextMenu: state.contextMenu,
    setContextMenu: state.setContextMenu,
    isResizing: state.isResizing,
    resizeStartX: state.resizeStartX,
    resizeStartWidth: state.resizeStartWidth,
    setTreeWidth: state.setTreeWidth,
    treeWidth: state.treeWidth,
    setIsResizing: state.setIsResizing,
  },
});

export const buildWorkspaceViewModelParams = (
  {
    project,
    state,
    pathHelpers,
    search,
    selection,
    runState,
    codeNav,
    treeStateOps,
  }: UseProjectExplorerWorkspaceModelParams,
  actions: WorkspaceModelActions,
  navigation: WorkspaceModelNavigation,
): Parameters<typeof useProjectExplorerWorkspaceViewModel>[0] => ({
  project,
  tree: {
    treeWidth: state.treeWidth,
    treeScrollRef: state.treeScrollRef,
    entriesMap: state.entriesMap,
    expandedPaths: state.expandedPaths,
    loadingPaths: state.loadingPaths,
    selectedPath: state.selectedPath,
    selectedEntry: selection.selectedEntry,
    draggingEntryPath: state.draggingEntryPath,
    dropTargetDirPath: state.dropTargetDirPath,
    actionLoading: state.actionLoading,
    actionReloadPath: selection.actionReloadPath,
    contextMenu: state.contextMenu,
    actionMessage: state.actionMessage,
    actionError: state.actionError,
  },
  search: {
    searchQuery: search.searchQuery,
    searchCaseSensitive: search.searchCaseSensitive,
    searchWholeWord: search.searchWholeWord,
    searchResults: search.searchResults,
    searchLoading: search.searchLoading,
    searchError: search.searchError,
    searchTruncated: search.searchTruncated,
    activeSearchHitId: search.activeSearchHitId,
    activeSearchHitIndex: search.activeSearchHitIndex,
    totalSearchHits: search.totalSearchHits,
    previewTargetLine: search.previewTargetLine,
    previewTargetLineRevision: search.previewTargetLineRevision,
    canOpenPreviousSearchHit: search.canOpenPreviousSearchHit,
    canOpenNextSearchHit: search.canOpenNextSearchHit,
  },
  preview: {
    loadingFile: state.loadingFile,
    error: state.error,
    selectedFile: state.selectedFile,
    savingFile: state.savingFile,
    saveError: state.saveError,
  },
  codeNav: {
    navCapabilities: codeNav.navCapabilities,
    navCapabilitiesLoading: codeNav.navCapabilitiesLoading,
    navCapabilitiesError: codeNav.navCapabilitiesError,
    selectedToken: codeNav.selectedToken,
    selectedTokenLine: codeNav.selectedTokenLine,
    selectedTokenColumn: codeNav.selectedTokenColumn,
    navResult: codeNav.navResult,
    navRequestKind: codeNav.navRequestKind,
    navLoading: codeNav.navLoading,
    navError: codeNav.navError,
    activeNavLocationId: codeNav.activeNavLocationId,
    canGoBackFromNav: codeNav.canGoBackFromNav,
    documentSymbols: codeNav.documentSymbols,
    documentSymbolsLoading: codeNav.documentSymbolsLoading,
    documentSymbolsError: codeNav.documentSymbolsError,
    requestDocumentSymbols: codeNav.requestDocumentSymbols,
  },
  run: {
    runStatus: runState.runStatus,
    runCatalogLoading: runState.runCatalogLoading,
    runCatalogError: runState.runCatalogError,
    runEnvironment: runState.runEnvironment,
    runEnvironmentLoading: runState.runEnvironmentLoading,
    runEnvironmentError: runState.runEnvironmentError,
    runTargets: runState.runTargets,
    availableToolchainKinds: runState.availableToolchainKinds,
    selectedToolchainOptions: runState.selectedToolchainOptions,
    missingToolchainKinds: runState.missingToolchainKinds,
    customToolchainDrafts: runState.customToolchainDrafts,
    envVarsDraft: runState.envVarsDraft,
    commandPreview: runState.commandPreview,
    envPreview: runState.envPreview,
    environmentHints: runState.environmentHints,
    envVarsPlaceholder: runState.envVarsPlaceholder,
    terminalUiEnabled: runState.terminalUiEnabled,
    selectedRunTargetId: runState.selectedRunTargetId,
    setSelectedRunTargetId: runState.setSelectedRunTargetId,
    updateSelectedToolchain: runState.updateSelectedToolchain,
    updateCustomToolchainDraft: runState.updateCustomToolchainDraft,
    saveCustomToolchain: runState.saveCustomToolchain,
    setEnvVarsDraft: runState.setEnvVarsDraft,
    saveEnvVarsDraft: runState.saveEnvVarsDraft,
    setTerminalUiEnabled: runState.setTerminalUiEnabled,
    starting: runState.starting,
    stopping: runState.stopping,
    restarting: runState.restarting,
    deleting: runState.deleting,
    runnerMessage: runState.runnerMessage,
    runnerError: runState.runnerError,
    runnerDiagnosis: runState.runnerDiagnosis,
    runnerSuggestions: runState.runnerSuggestions,
    projectRunState: runState.projectRunState,
    projectRunInstances: runState.projectRunInstances,
    selectedRunInstanceId: runState.selectedRunInstanceId,
    projectRunTerminal: runState.projectRunTerminal,
    activeRun: runState.activeRun,
    lastExitedRun: runState.lastExitedRun,
    activeTerminalBusy: runState.activeTerminalBusy,
    selectRunInstance: runState.selectRunInstance,
    handleRunnerStart: runState.handleRunnerStart,
    handleRunnerStop: runState.handleRunnerStop,
    handleRunnerRestart: runState.handleRunnerRestart,
    handleRunnerDelete: runState.handleRunnerDelete,
    refreshRunnerState: runState.refreshRunnerState,
  },
  interactions: {
    normalizePath: pathHelpers.normalizePath,
    toExpandedKey: pathHelpers.toExpandedKey,
    canDropToDirectory: actions.canDropToDirectory,
    setSelectedPath: state.setSelectedPath,
    setSelectedFile: state.setSelectedFile,
    setDraggingEntryPath: state.setDraggingEntryPath,
    setDropTargetDirPath: state.setDropTargetDirPath,
    setMoveConflict: state.setMoveConflict,
    setContextMenu: state.setContextMenu,
    clearDragExpandTimer: actions.clearDragExpandTimer,
    cancelDragExpandIfMatches: actions.cancelDragExpandIfMatches,
    scheduleDragExpand: actions.scheduleDragExpand,
    clearDragAutoScroll: actions.clearDragAutoScroll,
    startDragAutoScroll: actions.startDragAutoScroll,
    selectProjectRoot: selection.selectProjectRoot,
    toggleDir: treeStateOps.toggleDir,
    openFile: selection.openFile,
    handleCreateDirectory: actions.handleCreateDirectory,
    handleCreateFile: actions.handleCreateFile,
    handleRefresh: actions.handleRefresh,
    handleSearchQueryChange: search.setSearchQuery,
    handleSearchCaseSensitiveChange: search.setSearchCaseSensitive,
    handleSearchWholeWordChange: search.setSearchWholeWord,
    handleSearchInProject: search.runSearchQuery,
    handleClearSearch: search.clearSearch,
    handleActivateSearchHit: search.activateSearchHit,
    handleOpenSearchHit: (hit) => search.handleOpenSearchHit(hit, selection.openFile),
    handleOpenPreviousSearchHit: () => search.openPreviousSearchHit(selection.openFile),
    handleOpenNextSearchHit: () => search.openNextSearchHit(selection.openFile),
    handleTokenSelection: navigation.handlePreviewTokenSelection,
    clearTokenSelection: codeNav.clearTokenSelection,
    requestDefinition: codeNav.requestDefinition,
    requestReferences: codeNav.requestReferences,
    handleOpenNavLocation: selection.openCodeNavLocation,
    goBackFromNav: codeNav.goBackFromNav,
    handleOpenDocumentSymbol: navigation.handleOpenDocumentSymbol,
    handleMoveEntryByDrop: actions.handleMoveEntryByDrop,
    handleDownloadSelected: actions.handleDownloadSelected,
    handleDeleteSelected: actions.handleDeleteSelected,
    handleCopyFilePath: actions.handleCopyFilePath,
    handleCopyRelativeFilePath: actions.handleCopyRelativeFilePath,
    handleIgnoreFile: async (entry) => actions.handleAppendGitignore(entry, 'file'),
    handleIgnoreFolder: async (entry) => actions.handleAppendGitignore(entry, 'folder'),
    handleIgnoreByExtension: async (entry) => actions.handleAppendGitignore(entry, 'extension'),
    handleOpenPathInDefaultProgram: async (entry) => actions.handleOpenExternally(entry, 'default'),
    handleRevealInFinder: async (entry) => actions.handleOpenExternally(entry, 'reveal'),
    handleOpenInCode: async (entry) => actions.handleOpenExternally(entry, 'code'),
    handleSaveFile: actions.handleSaveFile,
  },
});
