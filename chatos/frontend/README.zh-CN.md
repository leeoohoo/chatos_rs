# chatos/frontend

Chat OS 主 React 前端。

## Docker 栈启动

在仓库根目录执行：

```bash
docker/deploy.sh up
```

默认地址：`http://localhost:8088`

## 只开发前端

```bash
npm install
npm run dev
```

## 检查

```bash
npm run build
npm run type-check
npm run test -- --run
npm run lint
```
