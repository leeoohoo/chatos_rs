<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows10.0.19041.0</TargetFramework>
    <TargetPlatformMinVersion>10.0.19041.0</TargetPlatformMinVersion>
    <RootNamespace>ChatOS.Desktop</RootNamespace>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <Platforms>x64;ARM64</Platforms>
    <RuntimeIdentifiers>win-x64;win-arm64</RuntimeIdentifiers>
    <UseWinUI>true</UseWinUI>
    <!--
      The out-of-process XamlCompiler.exe bundled with Windows App SDK 1.7 has
      broken error resources on non-English Windows installations. It exits
      with code 1 and hides the actual diagnostic. The SDK's in-process task
      uses the same compiler and works with dotnet/MSBuild without depending on
      the machine UI language or a Visual Studio installation.
    -->
    <UseXamlCompilerExecutable>false</UseXamlCompilerExecutable>
    <EnableMsixTooling>true</EnableMsixTooling>
    <WindowsPackageType>None</WindowsPackageType>
    <WindowsAppSDKSelfContained>true</WindowsAppSDKSelfContained>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\ChatOS.Core\ChatOS.Core.csproj" />
    <ProjectReference Include="..\ChatOS.Api\ChatOS.Api.csproj" />
    <ProjectReference Include="..\ChatOS.Connector\ChatOS.Connector.csproj" />
    <ProjectReference Include="..\ChatOS.Presentation\ChatOS.Presentation.csproj" />
    <PackageReference Include="CommunityToolkit.Mvvm" />
    <PackageReference Include="Microsoft.Extensions.Hosting" />
    <PackageReference Include="Microsoft.WindowsAppSDK" />
  </ItemGroup>
  <ItemGroup>
    <Content Include="Assets\**\*.png" />
  </ItemGroup>
</Project>
