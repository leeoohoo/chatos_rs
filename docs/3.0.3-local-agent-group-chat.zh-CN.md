# ChatOS 3.0.3 本地 Agent 群聊实施方案

## 当前实现状态

截至当前 `3.0.3` 分支：

- 已完成本地 Agent、room、成员、消息和 delivery 领域模型；
- 已完成 SQLite schema、账户/项目隔离、稳定 mention ID 和事务路由；
- 已完成每 Agent 单活动 delivery 的 claim/complete/fail 状态机；
- 已完成 hop/run budget；
- 已完成进程内 `chat_get_trigger`、`chat_list_members`、`chat_read_messages`、`chat_send_message` 工具；
- 已完成 Agent 独立 `agent_project:{agent_id}:{project_id}` Memory scope；
- 已在项目工作区加入“Agent 群聊”页签及创建 room、创建 Agent、选择 mention、发送消息的首版 UI；
- 已完成 Store、Chat Tool Provider、Builder Tool Provider 的单元测试及本地运行时 smoke；
- 已完成本地 Scheduler、AgentRuntime 自动消费 delivery、checkpoint 持久化、Native Plugin Tool Broker、崩溃恢复和 Agent Builder 确认闭环；
- 后续重点：Memory 离线缓存/outbox、完整运行控制与历史检查器、不同 Agent worker 并行和多窗口全局租约。

## 1. 目标

3.0.3 在 ChatOS macOS 客户端内实现项目范围的多 Agent 群聊。群聊、消息、投递、运行调度和插件调用均在本机完成，不依赖 Codex CLI、Cloud Agent、Task Runner 或服务端编排。

服务端只承担可选的联网能力：

- Memory Engine 的召回、摘要和持久化；
- 账号、模型配置、插件目录及插件安装包分发。

已经安装到客户端的插件在 Agent 运行期间由本机 Plugin Runtime 直接启动和调用，不经过服务端中转。

## 2. 产品边界

项目是唯一协作边界，不引入公司、组织、员工账户或服务端团队模型。

```text
Local Project
  └── Agent Room
        ├── Human
        ├── Agent A + private memory
        ├── Agent B + private memory
        ├── Agent Builder + controlled creation tools
        └── shared local transcript
```

第一阶段只实现：

- 一个项目创建一个活跃 Agent 群聊；
- 添加已有 Agent 或手动创建 Agent；
- 通过内置 Agent Builder 生成 Agent 草案，用户确认后创建并加入群聊；
- 用户通过 `@Agent` 唤醒一个或多个 Agent；
- 未 `@` 时交给群聊默认 Agent；
- Agent 可以通过群聊工具读取消息、回复并 `@` 其他 Agent；
- 每个 Agent 独立 Memory；
- Agent 可以调用客户端已安装插件；
- 本地 checkpoint、取消、重试和崩溃恢复；
- 对自动互相唤醒设置明确预算。

第一阶段不实现：

- 公司与组织架构；
- 服务端群聊或服务端 Agent 调度；
- 独立任务系统和排期系统；
- 跨设备同步；
- App 完全退出后的后台执行；
- 自动 Git worktree 和自动合并。

## 3. 核心原则

### 3.1 群聊不是 Memory

群聊记录是本地 SQLite 中的共享事实源。Memory 是各 Agent 独立的长期认知，不用聊天记录替代，也不把整段群聊自动复制进长期 Memory。

MVP 使用以下 Memory scope：

```text
agent:{agent_id}
agent_project:{agent_id}:{project_id}
```

运行上下文固定绑定 `agent_id` 和 `project_id`。模型不能通过工具参数指定身份，因此 Agent A 无法读取或写入 Agent B 的 Memory。

Memory Engine 不可用时：

- 群聊和本地运行仍可继续；
- 使用最近一次成功召回的本地缓存；
- 待同步记录写入本地 outbox；
- 网络恢复后补传。

### 3.2 群聊是 MCP 语义的协作工具面

Agent 被唤醒时只获得轻量触发 envelope，不自动注入完整聊天历史：

```json
{
  "room_id": "room-id",
  "delivery_id": "delivery-id",
  "trigger_message_id": "message-id",
  "reason": "mentioned"
}
```

Agent 通过以下工具按需获取和发送信息：

```text
chat.list_members
chat.read_unread
chat.read_messages
chat.ack
chat.send_message
```

第一版将这些工具实现为进程内 `AgentToolProvider`，直接交给 `ChatOSAgentRuntime`。工具名称、参数和返回值保持 MCP 兼容。未来需要支持外部 Agent 时，可在同一个领域服务外增加 stdio/HTTP MCP adapter，无需重写业务逻辑。

Agent 的回复必须通过 `chat.send_message` 落库。本轮 Runtime 的 `completionCheck` 以 delivery 是否产生回复为准，防止模型文本未持久化或重试产生重复消息。

### 3.3 本地插件直接执行

新增 `LocalAgentToolBroker` 抽象，并由 `NativePluginToolBroker` 实现。实现复用现有：

- `NativePluginManifestLoader`；
- `NativePluginStdioClient`；
- `NativePluginRuntimeStore`；
- 工具权限与审批策略；
- Artifact 注册；
- Browser/Computer Use 可视会话与独占执行。

远程 Plugin Relay 与本地 Agent 共用执行内核：

```text
Remote Relay -> signature verification -> NativePluginToolBroker
Local Agent  -> trusted local run scope -> NativePluginToolBroker
```

本地 Agent 不伪造服务端签名，也不绕过权限、审批和审计。

## 4. 本地领域模型

### 4.1 Agent profile

```text
LocalAgentProfile
- id
- ownerUserID
- name
- description
- rolePrompt
- modelConfigID
- defaultPluginIDs
- defaultSkillIDs
- status
- createdAtUnixMs
- updatedAtUnixMs
```

Agent profile 可复用于多个项目。项目内权限和职责放在 room member 上，避免把项目路径或权限写进全局身份。

### 4.2 Room 与成员

```text
ProjectAgentRoom
- id
- ownerUserID
- projectID
- name
- goal
- defaultAgentID?
- status
- createdAtUnixMs
- updatedAtUnixMs

ProjectAgentRoomMember
- roomID
- agentID
- role
- responsibility
- pluginAllowlist
- status
- joinedAtUnixMs
```

MVP 对同一项目只允许一个 active room。

### 4.3 消息与投递

```text
ProjectAgentMessage
- id
- roomID
- senderKind       // human | agent | system
- senderID
- content
- replyToMessageID?
- sourceRunID?
- causationID?
- createdAtUnixMs

ProjectAgentDelivery
- id
- roomID
- messageID
- targetAgentID
- triggerKind      // mention | default_agent | agent_mention
- status           // pending | running | completed | failed | cancelled
- attempt
- hopCount
- deduplicationKey
- claimedAtUnixMs?
- completedAtUnixMs?
- createdAtUnixMs
```

投递由唯一 `deduplicationKey` 保证幂等。同一个 Agent 对同一条消息最多拥有一个有效 delivery。

### 4.4 运行记录

`LocalAgentRun` 保存：

- delivery、room、project 和 agent 的不可变绑定；
- `AgentRunCheckpoint`；
- policy 与模型配置快照；
- 工具清单摘要；
-状态、错误和时间戳。

工具调用 receipt 继续由 `AgentRunCheckpoint` 管理，副作用调用结果不确定时进入人工确认，不能自动重放。

## 5. 本地调度

`LocalAgentGroupChatCoordinator` 负责消息路由和运行触发。`LocalAgentScheduler` 负责执行并发：

- 同一个 Agent 串行；
- 不同 Agent 可并行；
- 同一 delivery 只能被一个 worker claim；
- App 重启后将遗留的 running delivery 恢复为待检查或 pending；
- 用户可以暂停本轮、取消单个 Agent 或停止全部 Agent。

Agent 互相唤醒限制：

```text
maximumHopCount = 4
maximumAgentRunsPerRootMessage = 12
sameAgentPerMessage = 1
```

超过预算时写入一条 system message，并等待用户继续，不自动扩展预算。

## 6. Agent Builder

Agent Builder 是客户端内置 profile，只能使用受控配置工具：

```text
project.inspect
model.list
plugin.list_installed
agent.draft
agent.create
agent.update
room.add_member
```

Builder 首先返回结构化 `LocalAgentDraft`。创建前由客户端展示确认卡片。可授予权限必须是项目权限、用户已授权插件权限和 Builder 工具策略的交集。Builder 不能创建另一个拥有 Agent 创建能力的普通 Agent。

## 7. 客户端 UI

项目工作区增加“Agent 群聊”页签：

```text
项目目录 | 用户消息 | Agent 群聊 | 项目设置
```

群聊页面包含：

- 消息流与回复关系；
- `@` 成员选择器；
- Agent 工作中、等待审批、失败和已停止状态；
- 可折叠工具调用记录与 Artifact；
- 成员侧栏；
- 添加已有 Agent、手动创建、让 Agent Builder 创建；
- 暂停本轮和停止全部 Agent。

## 8. 模块拆分

```text
ChatOSCore
  LocalAgentProfile / Room / Member / Message / Delivery
  LocalAgentGroupChatStore protocol

ChatOSConnector
  SQLiteAgentGroupChatStore
  LocalAgentGroupChatCoordinator
  LocalChatToolProvider
  NativePluginToolBroker
  MemorySyncAdapter

ChatOSApp
  AgentGroupChatViewModel
  AgentGroupChatView
  AgentEditorView
  AgentBuilderView
```

## 9. 交付阶段

### Phase 1：本地群聊内核

- 领域模型与校验；
- SQLite schema；
- 创建 room、成员和消息；
- `@` 解析、delivery 幂等创建、claim/complete/failed；
- 单元测试。

### Phase 2：单 Agent 闭环

- `LocalChatToolProvider`；
- 通用本地 Agent definition；
- AgentRuntime checkpoint；
- 默认 Agent 和 `@Agent` 唤醒；
- 项目内群聊 UI。

### Phase 3：Memory 与插件

- Agent 独立 Memory scope；
- 离线缓存和 outbox；
- `NativePluginToolBroker`；
- 审批、Artifact 和可视会话联动。

### Phase 4：多 Agent 与 Agent Builder

- Agent 间 `@` 唤醒；
- hop/run budget；
- Agent Builder 草案与确认；
- 崩溃恢复和完整运行控制。

## 10. 第一阶段验收标准

- 数据只写本地 SQLite；
- 同一项目无法创建两个 active room；
- 非 room 成员不能作为消息发送者或 delivery 目标；
- `@Agent` 使用稳定 Agent ID 解析，重名时不误投递；
- 重复路由不会产生重复 delivery；
- 同一个 Agent 只能 claim 一个活动 delivery；
- message、delivery 和状态变更在事务中提交；
- 删除/归档项目不会让 Agent 读取其他项目的数据；
- 测试不依赖网络、服务端或 Codex CLI。

## 11. `3.0.3` 当前实施状态

截至当前分支，以下链路已经落地：

```text
项目群聊消息（SQLite）
  → 本地 delivery queue / claim
  → LocalAgentGroupChatScheduler
  → AgentRuntime
  → 用户模型配置对应的模型 API
  → chat_get_trigger / chat_read_messages / chat_send_message
  → 回复和后续 @Agent delivery 写回 SQLite
```

已完成：

- `LocalAgentGroupChatRun` 与完整 `AgentRunCheckpoint` 的 SQLite 持久化；
- Run 身份固定绑定账户、项目、房间、Agent、delivery 和 root message；
- `AgentToolProviderRegistry` 合并群聊工具和后续本地插件工具，并拒绝工具名覆盖；
- 本地 Scheduler 消费项目成员队列，不通过服务端选择 Agent 或路由消息；
- 首次 Prompt 只携带 delivery envelope，不注入完整群聊记录；
- 每个 Agent 使用 `agent_project:{agent_id}:{project_id}` 独立 Memory subject；
- Memory Engine 暂时不可用时，新 delivery 仍可无 Memory 执行，并在 Run event 中明确记录；
- Agent 回复中使用稳定 Agent ID 的 mention 会在本地继续产生和消费下一跳 delivery；
- Agent profile 可显式选择本机已安装 Plugin；客户端直接启动其 stdio MCP，复用现有权限、审批、Artifact 和视觉会话运行时，不经过服务端 Relay 调度；
- 已完成内置 Agent Builder：通过受控的项目/模型/本机 Plugin 快照生成 `LocalAgentDraft`，先展示客户端确认卡片，用户确认后才创建并加入群聊；
- Builder 规划阶段不暴露创建或成员管理写工具，确认阶段由客户端重新读取模型与 Plugin allowlist，防止过期草案或模型参数越权；
- 手动创建 Agent 已改用可用模型选择器，不再要求用户手输模型配置 ID；
- UI 发送后后台启动 Scheduler，并在 transcript 中展示持久化回复；
- 重新打开项目时自动继续 pending delivery；running/paused/needs-review Run 显示恢复卡片，用户可沿用原 checkpoint 恢复或显式结束；
- 恢复时若 checkpoint 存在未确认的写工具调用，会保持 `needsReview`，不会自动重放副作用；
- SQLite 重开后 Run checkpoint、事件、消息和 delivery 状态仍可读取。

尚未完成：

- Plugin 选择器的搜索、分类和 Agent 创建后的权限编辑；
- Memory 离线缓存/outbox；
- 用户主动暂停、停止全部 Agent 和更细的 Run 历史检查器；
- 不同 Agent worker 的并行优化（当前按成员顺序消费，语义已经隔离）。

### Agent Builder 已实现的本地闭环

```text
用户描述所需角色
  → 内置 Builder 使用用户选择的模型
  → project_inspect / model_list / plugin_list_installed
  → agent_draft（terminal，只保存草案）
  → 客户端确认卡片
  → 重新校验当前模型与本机 Plugin allowlist
  → 客户端创建 Agent profile 并加入当前项目 room
```

Builder 自身使用稳定的 `builtin-agent-builder` 项目 Memory subject；新运行在 Memory Engine 暂不可用时仍可生成草案。普通 Agent 不会获得 Builder 工具，也不能在角色 Prompt 或 Plugin 参数中给自己添加创建 Agent、管理成员或额外项目权限。
