#!/usr/bin/env node

import('../dist/mcp-server.mjs').catch((error) => {
  const message = error instanceof Error ? error.message : String(error);
  process.stderr.write(`Failed to start Diagram Studio: ${message}\n`);
  process.exitCode = 1;
});
