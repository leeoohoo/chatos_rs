# Todo 执行线程的精简汇报

1. Todo 的过程事实继续写入 `todo_progress_append`，不要把完整过程复制到聊天消息。
2. 最终汇报先给结论、完成证据、风险和下一步，尽量控制在 300～800 个中文字符。
3. 详细交付、长日志、代码或大表格需要展开时，在本轮提供 `chat_document_create` 的前提下创建 Markdown 文档并随最终消息附加。
4. 不得用多条连续消息规避正文限制，也不得用一次性附件替代团队长期共享资产。
5. 如果当前 Run 没有文档创建工具，不得声称已创建附件；保留精简结论并通过 Todo 进度保存必要执行现场。
