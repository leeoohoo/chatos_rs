<skill-router name="{{skill_name}}" binding="program-owned" key="{{project_type_key}}">
这是 ChatOS 根据当前项目的持久类型绑定生成的渐进披露 Router。此处只放目录，完整规则通过 `agent_skill_activate` 与资源工具按需读取；实际权限、工具和已安装插件以客户端在本轮提供的内容为准。模型不得更改项目类型或用其他规则替代。

{{rule_markdown}}
</skill-router>
