# 3.0.4 推进记录：定时频率与进度文件规则

- 时间：2026-09-18 18:01:51 CST（Asia/Shanghai）
- 本轮目标：将 3.0.4 heartbeat 从每小时调整为每 30 分钟，并要求每个实际改动轮次新建独立进度记录。
- 起始提交：`49f2dffb1`
- 本轮变更提交：`d486ae149`

## 实际改动

1. 将 Codex 定时任务“推进 ChatOS 3.0.4 客户端重构”的频率调整为每 30 分钟。
2. 保留评审闸门：实施方案未获用户明确批准前，定时任务不得修改仓库。
3. 要求每个产生仓库改动的定时轮次在 `docs/progress/3.0.4/` 新建一份不可覆盖的时间戳文件。
4. 要求代码改动与进度记录分开提交，进度文件引用对应改动提交哈希；无改动轮次不创建空记录。
5. 将上述规则同步写入 3.0.4 实施方案。

## 涉及文件

- `docs/plans/3.0.4-native-client-refactor-and-parity-plan.zh-CN.md`
- Codex 应用内 heartbeat 配置 `chatos-3-0-4`（不属于仓库文件）

## 业务不变量

- 未修改任何客户端业务代码、数据库、协议或测试逻辑。
- 未批准实施方案前，自动化继续保持只检查、不改仓库。
- Mac 阶段完成前不得开始 Windows 功能移植。
- 测试账号仍只允许通过运行时 Secret 或环境变量注入。

## 验证结果

- Codex 自动化更新返回 `ACTIVE`。
- 新调度规则为每 30 分钟一次。
- 实施方案通过 `git diff --check`。
- 方案变更已独立提交为 `d486ae149`。

## 剩余风险

- 工作区存在 3 个非本轮产生的 macOS 并发改动，未纳入提交；后续自动化必须继续避开并确认其归属。
- heartbeat 依赖电脑开机、Codex 应用运行和本地项目可访问。
- 3.0.4 实施方案当前仍处于待评审状态。

## 下一步

等待用户明确批准实施方案；获批后从阶段 0 的行为冻结与 SQLite 历史 Schema 回归基线开始。
