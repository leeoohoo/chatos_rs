# Local Agent protocol v11 fixtures

These JSON documents are produced from the authoritative Rust protocol shape and
are consumed unchanged by the Rust, Swift, and C# conformance tests. Native
clients must not maintain private fixture copies or accept legacy field names.
